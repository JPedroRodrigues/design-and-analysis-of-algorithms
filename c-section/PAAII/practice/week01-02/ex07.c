#include <stdio.h>


int countDigit(int n, int k) {
    
}


int main() {
    int n = 32281730;
    int k = 3;
    int c = countDigit(n, 3);
    printf("Number of appearences of %d in %d: %d\n", k, n, c);
    return 0;
}